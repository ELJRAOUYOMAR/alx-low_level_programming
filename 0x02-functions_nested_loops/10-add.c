#include "main.h"

/**
 * add - adds two integers and returns the result
 * @a: the first integer
 * @b: the second integer
 *
 * Return: the sum of a and b
 */
int add(int a, int b)
{
int result;
result = a + b;
return (result);
}
